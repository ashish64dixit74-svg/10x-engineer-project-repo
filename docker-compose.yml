version: "3.9"

services:
  backend:
    build:
      context: ./backend
    container_name: fastapi_backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
    environment:
      - ENV=development
    command: >
      uvicorn app.api:app
      --host 0.0.0.0
      --port 8000
      --reload